#[Route('/bestellingen', name: 'order_overview')]
public function index(OrderRepository $repo): Response
{
    $this->denyAccessUnlessGranted('ROLE_ADMIN');

    $orders = $repo->findAll();

    return $this->render('order/index.html.twig', [
        'orders' => $orders,
    ]);
}